name: hw_36
description: "Clean Architecture + MVVM — BLoC, DI, Dio, Drift, animations."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  dio: ^5.8.0+1
  drift: ^2.26.0
  drift_flutter: ^0.2.4
  path: ^1.9.1
  path_provider: ^2.1.5
  path_provider_foundation: ^2.4.0
  path_provider_linux: ^2.2.1
  path_provider_windows: ^2.2.1
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.26.0
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
